export const environment = {
  production: true,
  // Para este proyecto, las URLs son las mismas,
  // pero en un futuro podrían ser URLs de una API real.
  datosPersonalesUrl: 'datos-personales.json',
  proyectosUrl: 'proyectos.json'
};